<?xml version="1.0" encoding="UTF-8" ?>
<pages:BasePage
    xmlns:pages="clr-namespace:Awful.Mobile.Pages"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Awful.Mobile.Views"
    Title="Bookmarks"
    x:Class="Awful.Mobile.Pages.BookmarksPage">
    <ContentPage.Content>
        <Grid>
            <Grid IsVisible="{Binding IsSignedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
                <RefreshView Command="{Binding RefreshCommand}"
                 IsRefreshing="{Binding IsRefreshing}">
                    <CollectionView x:Name="ThreadListCollection"
                        SelectionMode="Single"
                        SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference ThreadListCollection}}" 
                        SelectionChangedCommand="{Binding SelectionCommand}"
                        ItemTemplate="{StaticResource ThreadDataTemplate}"
                        ItemsSource="{Binding Threads}">
                    </CollectionView>
                </RefreshView>
            </Grid>
            <Grid IsVisible="{Binding IsSignedIn, Converter={StaticResource BooleanToVisibilityInverter}}">
                <views:AuthorizationView/>
            </Grid>
        </Grid>
    </ContentPage.Content>
</pages:BasePage>
